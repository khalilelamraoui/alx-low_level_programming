#include"main.h"
/*
 * main - entry point.
 * @argc: number of arguments.
 * @argv: string.
 * Return: name of program.
 */
int main(int argc, char *argv[])
{
	if (argc >= 1)
		printf("%s\n", argv[0]);
	return (0);
}
