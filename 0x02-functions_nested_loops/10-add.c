#include"main.h"
/**
 * add - Entry point
 * description: function that checks if char is lower
 * @a: first int
 * @b: second int
 * Return: a + b (Success)
 */
int add(int a, int b)
{
	return (a + b);
}
